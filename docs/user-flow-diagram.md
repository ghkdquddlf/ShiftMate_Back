# ShiftMate User Flow Diagram

ShiftMate 백엔드의 현재 컨트롤러/서비스 기준 사용자 흐름을 Mermaid로 정리한 문서입니다.

- 기준 소스: `src/main/java/com/example/shiftmate/domain/**/controller`, `README.md`
- 관점: API 나열이 아니라 실제 사용자 행동 흐름 중심
- 역할 분기: `MANAGER` 와 `STAFF/PART_TIME`

```mermaid
flowchart TD
    Start(["ShiftMate 시작"])

    subgraph Auth["1. 인증 / 계정 준비"]
        A1{"로그인 방식 선택"}
        A2["회원가입 이메일 인증 코드 요청"]
        A3["이메일 인증 코드 확인"]
        A4["로컬 회원가입"]
        A5["로그인"]
        A6["카카오 / 구글 소셜 로그인"]
        A7{"프로필 완료 여부"}
        A8["이름 / 전화번호 입력"]
        A9["Access / Refresh 토큰 발급"]
        A10["비밀번호 재설정"]
        A11["내 매장 목록 조회"]
        A12{"소속 매장 있음?"}
        A13{"매장 내 역할?"}

        Start --> A1
        A1 -->|로컬 회원가입| A2
        A2 --> A3
        A3 --> A4
        A4 --> A5
        A1 -->|기존 계정 로그인| A5
        A1 -->|소셜 로그인| A6
        A1 -->|비밀번호 찾기| A10
        A10 --> A5
        A5 --> A9
        A6 --> A7
        A7 -->|미완료| A8
        A7 -->|완료| A9
        A8 --> A9
        A9 --> A11
        A11 --> A12
    end

    subgraph PreStore["2. 매장 진입 전 분기"]
        P1["사업자번호 검증"]
        P2["매장 생성"]
        P3["관리자 계정으로 매장 첫 진입"]
        P4["관리자 초대 대기"]
        P5["초대 후 매장 목록에 표시"]

        A12 -->|없음, 운영자| P1
        P1 --> P2
        P2 --> P3
        P3 --> A13

        A12 -->|없음, 직원| P4
        P4 --> P5
        P5 --> A13

        A12 -->|있음| A13
    end

    subgraph Manager["3. 관리자(MANAGER) Flow"]
        M1["매장 정보 조회 / 수정 / 삭제"]
        M2["매장 이미지 업로드 / 미리보기 / 삭제"]
        M3["직원 이메일로 사용자 조회"]
        M4["매장 직원 등록 / 수정 / 삭제"]
        M5["직원 문서 조회 / 미리보기 / 다운로드"]
        M6["시프트 템플릿 생성"]
        M7["템플릿 타입 설정"]
        M8["템플릿별 필요 인원 설정"]
        M9["직원 선호도 등록 / 조회 / 수정 / 삭제"]
        M10["주간 스케줄 자동 생성"]
        M11["전체 스케줄 조회 / 주차 삭제"]
        M12["일간 / 주간 근태 조회"]
        M13["직원 OTP로 출퇴근 처리"]
        M14["대타 요청 전체 조회"]
        M15["대타 지원 목록 조회"]
        M16["대타 승인 / 거절 / 관리자 취소"]
        M17["오픈시프트 생성"]
        M18["오픈시프트 지원자 조회"]
        M19["오픈시프트 승인"]
        M20["내 정보 / 비밀번호 수정"]
        M21["월별 급여 조회"]

        A13 -->|MANAGER| M1
        M1 --> M2
        M1 --> M3
        M3 --> M4
        M4 --> M5
        M4 --> M9
        M1 --> M6
        M6 --> M7
        M6 --> M8
        M7 --> M10
        M8 --> M10
        M9 --> M10
        M10 --> M11
        M11 --> M12
        M12 --> M13
        M11 --> M14
        M14 --> M15
        M15 --> M16
        M11 --> M17
        M17 --> M18
        M18 --> M19
        M1 --> M20
        M20 --> M21
    end

    subgraph Staff["4. 직원(STAFF / PART_TIME) Flow"]
        S1["내 정보 조회 / 프로필 수정 / 비밀번호 변경"]
        S2["내 문서 업로드 / 조회 / 삭제 / 미리보기"]
        S3["내 매장 선택"]
        S4["내 스케줄 조회"]
        S5["출퇴근용 OTP 발급"]
        S6["관리자가 OTP로 출퇴근 처리"]
        S7["내 일간 / 주간 근태 조회"]
        S8["내 스케줄 기준 대타 요청 생성"]
        S9["내 대타 요청 조회 / 취소"]
        S10["다른 직원 대타 요청 조회"]
        S11["대타 지원"]
        S12["내 대타 지원 내역 조회 / 취소"]
        S13["오픈시프트 목록 조회"]
        S14["오픈시프트 지원"]
        S15["내 오픈시프트 지원 내역 조회"]
        S16["월별 급여 조회"]

        A13 -->|STAFF / PART_TIME| S1
        S1 --> S2
        S1 --> S3
        S3 --> S4
        S4 --> S5
        S5 --> S6
        S6 --> S7
        S4 --> S8
        S8 --> S9
        S4 --> S10
        S10 --> S11
        S11 --> S12
        S4 --> S13
        S13 --> S14
        S14 --> S15
        S1 --> S16
    end
```

## 흐름 요약

- 공통 진입점은 `로컬 로그인/회원가입`, `소셜 로그인`, `비밀번호 재설정` 입니다.
- 소셜 로그인 사용자는 최초 로그인 후 `프로필 완료(profileCompleted)` 여부에 따라 추가 정보 입력 흐름이 있습니다.
- 매장 소속이 없는 사용자는 운영자라면 `사업자번호 검증 -> 매장 생성`, 직원이라면 `초대 대기` 흐름으로 분기됩니다.
- 관리자 흐름은 `매장 관리 -> 직원 관리 -> 템플릿/선호도 관리 -> 스케줄 생성 -> 근태/대타/오픈시프트 운영` 순서로 연결됩니다.
- 직원 흐름은 `내 정보/문서 관리 -> 스케줄 확인 -> OTP 발급 -> 근태 확인 -> 대타/오픈시프트 참여 -> 급여 조회` 중심입니다.

## 참고

- 전체 ERD: [shiftmate-erd.md](./shiftmate-erd.md)
- 도메인 분리 ERD: [shiftmate-domain-erd.md](./shiftmate-domain-erd.md)
- 시프트 전용 ERD: [shift-planning-erd.md](./shift-planning-erd.md)
