# Development profile
# - Keep existing data
# - Let Hibernate add newly introduced tables/columns
# - Do not run data.sql automatically
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false
